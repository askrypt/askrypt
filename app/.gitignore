# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml

# android/ and ios/ ARE tracked: they carry customized native config
# (app ID com.askrypt.app, minSdk 26, app name) and will hold hand-written
# autofill code in Phase 5. Their own .gitignore files exclude build output.
# Desktop/web targets are not built for this app; ignore if ever generated.
/linux/
/macos/
/windows/
/web/

# IDE
.idea/
.vscode/
